<h1>Your list</h1>
<ul>
<% @tasks.each do |item|%>
  <span> <%= check_box_tag "", item.completed, item.completed %> </span>
 <li><%= link_to item.title, task_path(item) %></li>
<%end%>
</ul>

<%= link_to "Add a new task", tasks_new_path %>
